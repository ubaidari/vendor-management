# Copy to "backend/.env". Do not commit "backend/.env".
# Local Postgres default. For Neon (or other hosts), paste the URL from the provider dashboard and rotate the password if it was ever exposed.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vendor_management?schema=public"
PORT=4000
